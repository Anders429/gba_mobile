[package]
name = "gba_mobile"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 3
debug = true

[profile.release]
lto = true

[dev-dependencies]
gba_test = {version = "0.5.0", features = ["log"]}
mgba_log = "0.2.1"
